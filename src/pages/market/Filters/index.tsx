import FilterContainer from "./FilterContainer";

const Filters = () => {
  return (
    <aside className="lg:flex hidden">
      <FilterContainer />
    </aside>
  );
};

export default Filters;
